import 'package:flutter/material.dart';

class CustomBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Light Music"),
    );
  }
}
